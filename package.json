{
  "name": "georlist",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": " node --loader ts-node/esm index.ts",
    "compile": "node --loader ts-node/esm compile.ts",
    "dev": "nodemon --exec node --loader ts-node/esm index.ts",
    "server": "node --loader ts-node/esm server.ts",
    "cron": "node --loader ts-node/esm cron.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@adguard/hostlist-compiler": "^1.0.39",
    "abort-controller": "^3.0.0",
    "ajv-formats": "^3.0.1",
    "date-fns": "^4.1.0",
    "node-cron": "^3.0.3",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@types/node-cron": "^3.0.11",
    "ajv": "^8.17.1",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
